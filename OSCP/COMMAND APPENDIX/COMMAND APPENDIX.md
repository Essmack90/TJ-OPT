# Command Appendix

A pure by-tool/by-area index. Not phase-ordered like [[METHODOLOGY CHEAT SHEET]], not symptom-ordered like [[DECISION TREE]], not a "why does this work" teardown like [[COMMAND BREAKDOWNS]], just: "I know roughly what I want to do, what's the exact syntax and where did we use it."

Split into one file per area (restructured 2026-08-04 from a single flat file, same pattern as [[COMMAND BREAKDOWNS]]) so it stays scannable as it grows. Every entry links back to the module section it came from for the full explanation and context.

## Areas

- [[Reconnaissance & Enumeration]] — WHOIS, Google dorking, passive OSINT, DNS/SMB/SMTP/SNMP enumeration, Nmap, Gobuster, Metasploit quick reference; FTP enumeration/attack; rpcclient/enum4linux/smbclient null session; smtp-user-enum RCPT + nc POP3; subbrute DNS subdomain brute force; gobuster vhost + nikto + scrapy/ReconSpider; OpenVAS workflow
- [[File Transfers]] — Windows (iwr/DownloadFile/DownloadString+IEX/certutil/bitsadmin/BitsTransfer/Expand-Archive/evil-winrm up-download), Linux (wget/curl/python urlretrieve/scp/nc pipe/bash /dev/tcp), upload (uploadserver/curl/iwr REST), RDP drive mount (xfreerdp /drive:)
- [[Web Requests & Delivery]] — Curl (requests/payload delivery), Python HTTP Server
- [[File Inclusion & Traversal]] — directory traversal, LFI, RFI, PHP wrappers, null-byte bypass
- [[File Upload Attacks]] — filter bypasses, PowerShell-via-upload reverse shell, upload+traversal `authorized_keys` overwrite
- [[Shells & Payloads]] — webshells (PHP/ASPX/CFM/Laudanum ASPX/Antak), reverse shells (bash/PS TCPClient one-liner/Powercat), bind shells (mkfifo), SSH key theft/planting, LOLBIN downloaders, cron-based delivery, Tomcat WAR (msfvenom java/jsp_shell_reverse_tcp), MSF session management (msfdb/db_nmap/setg/background/sessions/SESSION chaining)
- [[SQL Injection & Databases]] — MySQL, MSSQL (impacket-mssqlclient + sqlcmd + xp_cmdshell + xp_dirtree hash coercion + impersonation via sys.server_permissions + linked server EXECUTE...AT), PostgreSQL, SQLi payloads, sqlmap
- [[Web Applications]] — Burp Suite (proxy/Repeater/Intruder/Decoder/Payload Processing pipeline/Ctrl+U URL-encode/PROXIES tool routing), ZAP (Fuzzer with MD5 Hash processor/Active Scanner + Alerts triage/Replacer for client-side restriction bypass), WordPress, Webmin, command injection diagnosis
- [[Phishing]] — website cloning, clone-patching (BeautifulSoup), credential capture servers
- [[Client-Side Attacks]] — WsgiDAV/WebDAV setup, Windows library file XML, `.lnk` shortcut payloads, VBA macros, one-shot SMB delivery
- [[Locating Public Exploits]] — SearchSploit syntax, patching a found exploit's hardcoded values, cewl targeted wordlists, CSRF-aware patator brute forcing
- [[Fixing Exploits]] — cross-compiling with `mingw-w64`, running Windows binaries via `wine`, multi-file SearchSploit copies, hex-decode pipelines
- [[Buffer Overflow & Memory Corruption]] — `msfvenom` shellcode generation for BOF payloads (bad chars, encoders, output formats), offset-discovery basics, Windows post-exploitation file search

- [[Password Attacks]] — Hydra (SSH/RDP/HTTP/SMTP/POP3/FTP -t 1), Hashcat (dict/rules/mask -a 3/psafe3 -m 5200/--stdout mutation/--example-hashes/--show), JtR (keepass2john/ssh2john/office2john/bitlocker2john/single-crack/unshadow), Mimikatz (privilege chain/memssp/sekurlsa::pth/sekurlsa::tickets/kerberos::ptt), Responder, impacket-ntlmrelayx, Pass-the-Hash (impacket/xfreerdp /pth: + DisableRestrictedAdmin/Invoke-WMIExec), BitLocker VHD chain (losetup/dislocker/mount), SAM offline dump (reg.exe+smbserver+secretsdump), pypykatz lsa minidump, NetExec --lsa/--ntds, cmdkey/runas /savecred/LaZagne, findstr cred hunting, NTDS via VSS, username-anarchy, kerbrute, Pass-the-Ticket (Windows: sekurlsa::tickets+kerberos::ptt / Linux: ccache+keytabextract+kinit+smbclient -k), Pass-the-Certificate (pywhisker+gettgtpkinit.py+evil-winrm -r)
- [[Windows Privilege Escalation]] — situational awareness, sensitive info hunting (PSReadLine/transcripts/registry), winPEAS/PowerUp/Seatbelt, service binary hijacking, DLL hijacking (nostdlib MinGW compile), unquoted service paths, scheduled task binary replacement, kernel exploits (CVE-2023-29360 / CVE-2023-28252), SeImpersonatePrivilege (SigmaPotato), SeBackupPrivilege (hive dump + FILE_FLAG_BACKUP_SEMANTICS), AlwaysInstallElevated
- [[Linux Privilege Escalation]] — manual enumeration checklist (id/passwd/uname/ps/ss/cron/SUID), unix-privesc-check/LinPEAS, credential hunting (env/dotfiles/crunch+hydra/tcpdump loopback), cron job abuse (mkfifo reverse shell injection), /etc/passwd world-writable exploitation, SUID GTFOBins (find/bash/python), capabilities (cap_setuid+ep/gdb/perl), sudo GTFOBins (apt-get/gcc/vim/find), kernel exploit workflow (searchsploit/compile on target/PwnKit CVE-2021-4034)
- [[Port Redirection and SSH Tunneling]] — Socat port forward (fork syntax, no-socat fallback), SSH local/dynamic/remote/remote-dynamic port forwards, sshuttle (VPN-like transparent routing), proxychains config + mandatory nmap flags (-sT/-Pn/-n), Hashcat mode 12001 (Atlassian PBKDF2), psql enumeration through a forward, ssh.exe (Windows OpenSSH identical syntax), Plink (-l/-pw flags, echo-y pipe trick for non-TTY), Netsh portproxy (v4tov4 add/show/del + advfirewall rule), subnet enumeration via nc loop; Meterpreter tunneling (autoroute + socks_proxy + ping sweep), Socat + Meterpreter bind_tcp, Rpivot HTTP tunnel (server.py/client.py), Dnscat2 DNS tunnel (ruby server + dnscat2-powershell client), Chisel SOCKS5 forward variant (server on pivot host), ptunnel-ng ICMP tunnel (static build + server/client + ssh -p2222), SocksOverRDP + Proxifier (Windows RDP multi-hop)

- [[Active Directory]] — external recon (TXT/TLS cert), LLMNR poisoning (Responder/Inveigh), password policy (getdompwinfo/--pass-pol), username enum (kerbrute/rpcclient queryuser), password spraying (kerbrute/CrackMapExec/DomainPasswordSpray.ps1), credentialed enumeration (bloodhound-python/Snaffler -d/PowerView full suite/setspn.exe), ACL enum (Get-DomainObjectACL -ResolveGUIDs/Convert-NameToSid), ACL abuse chain (Set-DomainUserPassword/Add-DomainGroupMember/Set-DomainObject SPN/targeted Kerberoast), Kerberoasting (GetUserSPNs.py/Rubeus), AS-REP Roasting (GetNPUsers.py/Rubeus /format:hashcat), DCSync (lsadump::dcsync/secretsdump/reversible-encryption hunt/runas /netonly), privileged access (BloodHound Cypher CanPSRemote/SQLAdmin/mssqlclient -windows-auth), NoPac (CVE-2021-42278/42287 scanner+exploit), domain trust enumeration (Get-DomainTrustMapping/netdom), ExtraSids golden ticket (Rubeus golden /sids:), raiseChild.py, cross-forest Kerberoasting (Rubeus /domain:/smbexec.py), LotL (Get-MpComputerStatus/dsquery LDAP filter)

*(Note: JuicyPotato, GPP/cPassword decryption, and Kerberoasting appeared in the [[Arctic]] and [[Active]] box writeups -- they are now covered by the Active Directory appendix above for Kerberoasting and the Windows Privilege Escalation appendix for the SeImpersonatePrivilege path.)*

#### Tags: #CommandAppendix #Methodology
