# Command Appendix

A pure by-tool/by-area index. Not phase-ordered like [[METHODOLOGY CHEAT SHEET]], not symptom-ordered like [[DECISION TREE]], not a "why does this work" teardown like [[COMMAND BREAKDOWNS]], just: "I know roughly what I want to do, what's the exact syntax and where did we use it."

Split into one file per area (restructured 2026-08-04 from a single flat file, same pattern as [[COMMAND BREAKDOWNS]]) so it stays scannable as it grows. Every entry links back to the module section it came from for the full explanation and context.

## Areas

- [[Common Applications]] — application discovery (EyeWitness/Aquatone), WordPress (WPScan enum/user enum/xmlrpc brute/plugin LFI), Joomla (README.txt version/brute/template RCE), Drupal (CHANGELOG.txt/PHP Filter RCE), Tomcat (MSF mgr_login brute/WAR RCE/CGI CVE-2019-0232), Jenkins (Groovy Script Console RCE), Splunk (app-install tar.gz RCE), PRTG (notification execute program), GitLab (user enum/authenticated RCE), Shellshock CGI, ColdFusion, IIS Tilde enumeration, LDAP wildcard bypass, mass assignment, gdb ODBC binary debugging, WebLogic, Nagios XI, dnSpy .NET credential extraction, **AEN additions**: DNN/DotNetNuke (xp_cmdshell via SQL Console + ASP webshell upload + whitelist extension bypass + PrintSpoofer SYSTEM + SAM offline dump + DefaultPassword from secretsdump)

- [[Reconnaissance & Enumeration]] — WHOIS, Google dorking, passive OSINT, DNS/SMB/SMTP/SNMP enumeration, Nmap, Gobuster, Ffuf (directory/extension/recursive/vhost/param/value fuzzing + -ac/-mr filtering), Metasploit quick reference; FTP enumeration/attack; rpcclient/enum4linux/smbclient null session; smtp-user-enum RCPT + nc POP3; subbrute DNS subdomain brute force; gobuster vhost + nikto + scrapy/ReconSpider; OpenVAS workflow
- [[Cloud Enumeration]] — AWS CLI setup + named profiles, DNS recon (host/dig TXT/dnsenum), S3 bucket enumeration (direct object access, curl -H Host, naming-convention loops, cloud_enum), EC2 (describe-images/describe-snapshots/describe-vpcs JMESPath filters), IAM (sts get-caller-identity, list-user/group policies, get-account-authorization-details single-call full dump), jq IAM dump analysis (group/attached/inline comparison, ABAC condition search), Pacu (import_keys/iam__enum_roles oracle/iam__enum_users_roles_policies_groups/assume_role/export_keys)
- [[File Transfers]] — Windows (iwr/DownloadFile/DownloadString+IEX/certutil/bitsadmin/BitsTransfer/Expand-Archive/evil-winrm up-download), Linux (wget/curl/python urlretrieve/scp/nc pipe/bash /dev/tcp), upload (uploadserver/curl/iwr REST), RDP drive mount (xfreerdp /drive:)
- [[Web Requests & Delivery]] — Curl (requests/payload delivery), Python HTTP Server
- [[File Inclusion & Traversal]] — directory traversal, LFI (plain/URL-encoded/non-recursive `....//` bypass/double URL-encoding `%252E%252E%252F`), php://filter base64 source disclosure + grep/sed extraction pipeline, data:// with base64 payload + URL-encode pipeline, log poisoning (Apache access.log User-Agent via Burp), PHP session file poisoning (PHPSESSID → `/var/lib/php/sessions/sess_ID` → URL-encoded webshell), GIF magic bytes + LFI upload (`GIF8<?php...?>`), RFI (python3 http.server), automated scanning (ffuf param discovery + LFI-Jhaddix.txt), md5_file upload filename prediction, null-byte bypass
- [[File Upload Attacks]] — webshell creation (`$_REQUEST` parameterised), extension bypass ladder (direct → case-swap → `.phar`/legacy → double extension `shell.php.jpg` → reverse double `shell.phar.jpg`), Burp Intruder extension fuzzing (PHP extensions list, disable URL encoding), client-side bypass (Burp intercept filename swap + DevTools form attribute edit), Content-Type header bypass + content-type fuzzing (`web-all-content-types.txt`), MIME/magic bytes bypass (`GIF8` prefix), SVG XXE file read + php://filter source disclosure + SVG+PHP polyglot RCE, upload path discovery via source, date-prefixed filename prediction, PowerShell reverse shell via upload, authorized_keys traversal
- [[Shells & Payloads]] — webshells (PHP/ASPX/CFM/Laudanum ASPX/Antak), reverse shells (bash/PS TCPClient one-liner/Powercat), bind shells (mkfifo), SSH key theft/planting, LOLBIN downloaders, cron-based delivery, Tomcat WAR (msfvenom java/jsp_shell_reverse_tcp), MSF setup + session management (msfdb/db_nmap/workspace/setg/background/sessions/run -j/-z), msfvenom payload generation (EXE/ELF/PHP/WAR/ASPX formats, staged vs non-staged), Meterpreter core commands (getsystem/migrate/load kiwi/creds_msv/portfwd/getenv), resource scripts (AutoRunScript/ExitOnSession/run -z -j)
- [[SQL Injection & Databases]] — MySQL, MSSQL (impacket-mssqlclient + sqlcmd + xp_cmdshell + xp_dirtree hash coercion + impersonation via sys.server_permissions + linked server EXECUTE...AT), PostgreSQL, SQLi payloads (UNION/ORDER BY column count, super_priv + FILE privilege + secure_file_priv checks, LOAD_FILE, INTO OUTFILE short webshell `<?=\`$_GET[0]\`?>`, Nginx/Apache config path for web root discovery), sqlmap (basic + advanced injection points: `*` marker/cookie/JSON/-r file; attack tuning: --level/--risk/--prefix/--union-cols/--technique; WAF bypass: --random-agent/--csrf-token/--randomize/--tamper=between; enumeration: --search -C/-T/-D/output directory; OS exploitation: --file-read/--os-shell --technique=E)
- [[Web Applications]] — Burp Suite (proxy/Repeater/Intruder/Decoder/Payload Processing pipeline/Ctrl+U URL-encode/PROXIES tool routing), ZAP (Fuzzer with MD5 Hash processor/Active Scanner + Alerts triage/Replacer for client-side restriction bypass), XSS (Stored/Reflected/DOM probe chars + cookie theft `alert(document.cookie)` + phishing via `document.write()` form injection + session hijacking via `new Image().src` + PHP cookie/cred capture server + blind XSS per-field fingerprinting), WordPress, Webmin, command injection diagnosis, command injection operator table (`;`/`%0a`/`&`/`&&`/`|`/`||` — which output each shows), command injection filter bypass ladder (space: `$IFS`/`%09`/brace expansion; slash: `${PATH:0:1}`; command blacklist: quote insertion `c'a't`; all filters: base64 + `bash<<<$(base64 -d<<<B64)` here-string), HTTP Verb Tampering (OPTIONS/PATCH to bypass basic auth + method swap to bypass POST-only filters), IDOR mass enumeration (bash loop + curl/wget, encoded refs: base64/MD5 reproduce client-side, API path IDOR, chain read→write for account takeover), XXE (inline entity file read, php://filter base64 source disclosure, CDATA external DTD for XML-breaking chars, error-based leak, blind OOB with python HTTP server)
- [[Phishing]] — website cloning, clone-patching (BeautifulSoup), credential capture servers
- [[12. Client-Side Attacks|Client-Side Attacks]] — WsgiDAV/WebDAV setup, Windows library file XML, `.lnk` shortcut payloads, VBA macros, one-shot SMB delivery
- [[13. Locating Public Exploits|Locating Public Exploits]] — SearchSploit syntax, patching a found exploit's hardcoded values, cewl targeted wordlists, CSRF-aware patator brute forcing
- [[14. Fixing Exploits|Fixing Exploits]] — cross-compiling with `mingw-w64`, running Windows binaries via `wine`, multi-file SearchSploit copies, hex-decode pipelines
- [[Buffer Overflow & Memory Corruption]] — `msfvenom` shellcode generation for BOF payloads (bad chars, encoders, output formats), offset-discovery basics, Windows post-exploitation file search

- [[16. Password Attacks|Password Attacks]] — Hydra (SSH/RDP/HTTP/SMTP/POP3/FTP -t 1), Hashcat (dict/rules/mask -a 3/psafe3 -m 5200/--stdout mutation/--example-hashes/--show), JtR (keepass2john/ssh2john/office2john/bitlocker2john/single-crack/unshadow), Mimikatz (privilege chain/memssp/sekurlsa::pth/sekurlsa::tickets/kerberos::ptt), Responder, impacket-ntlmrelayx, Pass-the-Hash (impacket/xfreerdp /pth: + DisableRestrictedAdmin/Invoke-WMIExec), BitLocker VHD chain (losetup/dislocker/mount), SAM offline dump (reg.exe+smbserver+secretsdump), pypykatz lsa minidump, NetExec --lsa/--ntds, cmdkey/runas /savecred/LaZagne, findstr cred hunting, NTDS via VSS, username-anarchy, kerbrute, Pass-the-Ticket (Windows: sekurlsa::tickets+kerberos::ptt / Linux: ccache+keytabextract+kinit+smbclient -k), Pass-the-Certificate (pywhisker+gettgtpkinit.py+evil-winrm -r), Medusa (SSH/FTP syntax + vs-Hydra table), CUPP (interactive mode + grep complexity filtering)
- [[17. Windows Privilege Escalation|Windows Privilege Escalation]] — situational awareness, sensitive info hunting (PSReadLine/transcripts/registry), winPEAS/PowerUp/Seatbelt, service binary hijacking, DLL hijacking (nostdlib MinGW compile), unquoted service paths, scheduled task binary replacement, kernel exploits (CVE-2023-29360 / CVE-2023-28252), SeImpersonatePrivilege (SigmaPotato/PrintSpoofer via MSSQL xp_cmdshell), SeBackupPrivilege (hive dump + FILE_FLAG_BACKUP_SEMANTICS / SeBackupPrivilegeCmdlets.dll), AlwaysInstallElevated, **HTB additions**: AppLocker enumeration (Get-AppLockerPolicy), named pipe ACL inspection (accesschk -w \pipe\), query user (session type), SeDebugPrivilege (procdump lsass + mimikatz sekurlsa::minidump), SeTakeOwnershipPrivilege (takeown + icacls + EnableAllTokenPrivs.ps1), SeLoadDriverPrivilege (EoPLoadDriver + Capcom.sys), Event Log Readers (wevtutil process creation log credential mining), DnsAdmins (dnscmd serverlevelplugindll), Server Operators (sc config binPath hijack), credential hunting (findstr /SIM / Sticky Notes plum.sqlite / Get-LocalUser Description / cmdkey / unattend.xml / PSReadLine), LaZagne/SharpChrome/SessionGopher/mRemoteNG decrypt, Firefox cookie theft (cookieextractor.py), Restic backup SAM/SYSTEM extraction, SCF file attack (Responder NetNTLMv2), CVE-2021-36934 (HiveNightmare), PrintNightmare CVE-2021-1675, Sherlock.ps1 / windows-exploit-suggester (old OS), MS10-092 / MS16-032, PwDump8, Citrix breakout (Open dialog UNC + PowerUp MSI + Bypass-UAC), **AEN additions**: SysaxAutomation file-triggered task (SYSTEM payload via monitored folder)
- [[18. Linux Privilege Escalation|Linux Privilege Escalation]] — manual enumeration checklist (id/passwd/uname/ps/ss/cron/SUID), unix-privesc-check/LinPEAS, credential hunting (env/dotfiles/crunch+hydra/tcpdump loopback), cron job abuse (mkfifo reverse shell injection), /etc/passwd world-writable exploitation, SUID GTFOBins (find/bash/python), capabilities (cap_setuid+ep/gdb/perl/cap_dac_override via vim.basic → /etc/passwd write), sudo GTFOBins (apt-get/gcc/vim/find/busctl pager escape), kernel exploit workflow (searchsploit/compile on target/PwnKit CVE-2021-4034), **HTB additions**: path abuse (/tmp in PATH → fake binary), restricted shell escape (ssh -t "bash --noprofile"), privileged groups (adm log hunt / disk debugfs / shadow hash / lxd container escape / docker group chroot), LXD escape (privileged container + host mount), Docker escape (docker run -v /:/mnt chroot), Logrotate/logrotten race condition, NFS no_root_squash (mount + SUID bash), LD_PRELOAD injection (env_keep + _init() .so), shared object hijacking (writable RUNPATH), Python library hijacking (writable dist-packages module), sudo -u#-1 bypass (CVE-2019-14287), GNU Screen 4.5.0 (41154.sh), Dirty Pipe CVE-2022-0847, **AEN additions**: aureport --tty (audit log TTY credential extraction)
- [[19. Port Redirection and SSH Tunneling|Port Redirection and SSH Tunneling]] — Socat port forward (fork syntax, no-socat fallback), SSH local/dynamic/remote/remote-dynamic port forwards, sshuttle (VPN-like transparent routing), proxychains config + mandatory nmap flags (-sT/-Pn/-n), Hashcat mode 12001 (Atlassian PBKDF2), psql enumeration through a forward, ssh.exe (Windows OpenSSH identical syntax), Plink (-l/-pw flags, echo-y pipe trick for non-TTY), Netsh portproxy (v4tov4 add/show/del + advfirewall rule), subnet enumeration via nc loop; Meterpreter tunneling (autoroute + socks_proxy + ping sweep), Socat + Meterpreter bind_tcp, Rpivot HTTP tunnel (server.py/client.py), Dnscat2 DNS tunnel (ruby server + dnscat2-powershell client), Chisel SOCKS5 forward variant (server on pivot host), ptunnel-ng ICMP tunnel (static build + server/client + ssh -p2222), SocksOverRDP + Proxifier (Windows RDP multi-hop)

- [[Active Directory]] — external recon (TXT/TLS cert), LLMNR poisoning (Responder/Inveigh), password policy (getdompwinfo/--pass-pol), username enum (kerbrute/rpcclient queryuser), password spraying (kerbrute/CrackMapExec/Spray-Passwords.ps1/DomainPasswordSpray.ps1 + capstone.rule), credentialed enumeration (bloodhound-python/Snaffler -d/PowerView full suite/setspn.exe), manual LDAP (LDAPSearch function: DirectoryEntry + DirectorySearcher + samAccountType filter + nested group chain), domain shares & SYSVOL (Find-DomainShare/findstr cpassword/gpp-decrypt), ACL enum (Get-ObjectAcl/Get-DomainObjectACL -ResolveGUIDs/Convert-SidToName/Convert-NameToSid), ACL abuse chain (net group/net user simple GenericAll + Set-DomainUserPassword/Add-DomainGroupMember/Set-DomainObject SPN/targeted Kerberoast), Kerberoasting (impacket-GetUserSPNs/Rubeus + clock sync procedure for KRB_AP_ERR_SKEW), AS-REP Roasting (impacket-GetNPUsers/Rubeus /format:hashcat), Silver Ticket (kerberos::golden /ptt /target /service /rc4 — Module 23), DCSync (lsadump::dcsync/secretsdump/reversible-encryption hunt/runas /netonly), privileged access (BloodHound Cypher CanPSRemote/SQLAdmin/mssqlclient -windows-auth), NoPac (CVE-2021-42278/42287 scanner+exploit), domain trust enumeration (Get-DomainTrustMapping/netdom), ExtraSids golden ticket (Rubeus golden /sids:), raiseChild.py, cross-forest Kerberoasting (Rubeus /domain:/smbexec.py), LotL (Get-MpComputerStatus/dsquery LDAP filter), **Lateral Movement (Module 24)**: WMI CIM session (New-CimSessionOption DCOM + Invoke-CimMethod Win32_Process.Create), winrs one-liner, PsExec with creds, Pass-the-Hash (impacket-wmiexec -hashes), Overpass-the-Hash (sekurlsa::pth + net use + PsExec hostname), Pass-the-Ticket (sekurlsa::tickets /export + kerberos::ptt + Group 0 vs Group 2), DCOM MMC20.Application ExecuteShellCommand, Golden Ticket (kerberos::golden /krbtgt /ptt + misc::cmd), Shadow Copy chain (vshadow -nw -p + copy GLOBALROOT device path + reg save system + secretsdump LOCAL)

*(Note: JuicyPotato, GPP/cPassword decryption, and Kerberoasting appeared in the [[Arctic]] and [[Active]] box writeups -- they are now covered by the Active Directory appendix above for Kerberoasting and the Windows Privilege Escalation appendix for the SeImpersonatePrivilege path.)*

#### Tags: #CommandAppendix #Methodology
